package commands{	import io.ConfigLoader;		import com.hexagonstar.env.command.Command;	import com.hexagonstar.env.command.ICommandListener;		import flash.events.ErrorEvent;	import flash.events.Event;			/**	 * LoadConfigCommand Class	 * @author Sascha Balkau	 */	public class LoadConfigCommand extends Command	{		////////////////////////////////////////////////////////////////////////////////////////		// Properties                                                                         //		////////////////////////////////////////////////////////////////////////////////////////				private var _configLoader:ConfigLoader;						////////////////////////////////////////////////////////////////////////////////////////		// Public Methods                                                                     //		////////////////////////////////////////////////////////////////////////////////////////				/**		 * Creates a new LoadConfigCommand instance.		 */		public function LoadConfigCommand(listener:ICommandListener)		{			super(listener);		}						/**		 * Execute the command.		 */ 		override public function execute():void		{			super.execute();						_configLoader = new ConfigLoader();			_configLoader.addEventListener(Event.COMPLETE, onComplete);			_configLoader.addEventListener(ErrorEvent.ERROR, onError);			_configLoader.load();		}						////////////////////////////////////////////////////////////////////////////////////////		// Getters & Setters                                                                  //		////////////////////////////////////////////////////////////////////////////////////////				override public function get name():String		{			return "loadConfig";		}						////////////////////////////////////////////////////////////////////////////////////////		// Event Handlers                                                                     //		////////////////////////////////////////////////////////////////////////////////////////				/**		 * @private		 */		private function onComplete(e:Event):void		{			complete();		}						/**		 * @private		 */		private function onError(e:ErrorEvent):void		{			notifyError(e.text);			complete();		}						////////////////////////////////////////////////////////////////////////////////////////		// Private Methods                                                                    //		////////////////////////////////////////////////////////////////////////////////////////				/**		 * finish		 * @private		 */		override protected function complete():void		{			_configLoader.removeEventListener(Event.COMPLETE, onComplete);			_configLoader.removeEventListener(ErrorEvent.ERROR, onError);			_configLoader.dispose();			_configLoader = null;						super.complete();		}	}}